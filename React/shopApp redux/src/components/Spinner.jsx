import './Spinner.css';
import React from 'react';

function Spinner() {
    return (
        <div>
            <div className="spinner">Loading...</div>
        </div>
    );
}

export default Spinner;
